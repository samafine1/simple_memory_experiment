# Simple Memory Experiment

## Usage

1. Setup a virtual environment in the directory
```bash
python3 -m venv .venv
source .venv/bin/activate
pip install numpy matplotlib stim sinter ldpc stimbposd ipykernel bposd
```

2. Run notebook
